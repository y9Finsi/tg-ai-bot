# memory/frontend.md — Веб-админка (admin-linear)

## 1. Стек и технологии

- **Фреймворк:** React 19 (`admin-linear/src/App.jsx`, `main.jsx`)
- **Сборщик:** Vite (`admin-linear/vite.config.js`)
- **Стилизация:** Tailwind CSS v4 (`@tailwindcss/vite`), Linear Design System (Inter Variable, `#08090a` canvas, `#0e1013` surfaces, `#5e6ad2` accent)
- **Иконки:** `lucide-react` (строго нормализованы к `stroke-[1.5]`)
- **Интерактивные карты:** Leaflet (OpenStreetMap с кастомным темным инвертированным стилем `.osm-dark-tiles`)
- **Паттерны:** `better-ui` (концентрические радиусы, scale on press 0.96, явные transition-свойства, WCAG AA контраст) и `better-layout` (группировка пространством, full bleed карта, плавающий HUD с safe-area)
- **Команда сборки:** `npm run admin:build` (собирает в `public/admin-linear`)

---

## 2. Структура `admin-linear/`
 
```
admin-linear/
├── index.html                  # HTML шаблон приложения (Inter Variable, Leaflet)
├── vite.config.js              # Сборщик Vite -> ../public/admin-linear
└── src/
    ├── main.jsx                # React root
    ├── App.jsx                 # Корневой лейаут с табами ("Пульт" и "Карта СПб"), хоткеи T/Space/R/M
    ├── index.css               # Стили дизайн-системы Linear
    ├── lib/
    │   ├── api.js              # HTTP клиент с поддержкой x-admin-key и сессий
    │   ├── dateUtils.js        # Форматирование времени, дат и отрезков
    │   └── simulationConstants.js # Справочник локаций СПб, потребностей, фаз цикла
    └── components/
        ├── Header.jsx          # Верхняя панель (Табы, Live статус, шаг +15м, погода СПб, цикл, кошелек)
        ├── LeraStatusHero.jsx  # Хиро-карточка: аватар Леры, локация, погода, вайб, антислоп транзит и мысль
        ├── InventoryWidget.jsx # Отдельный блок пульта: сплит-инвентарь (надето/рюкзак), табы, действия, вызов ItemBuilderModal
        ├── ItemBuilderModal.jsx # Модалка крафта/добавления предметов Леры (пресеты, независимые категории, баффы/дебаффы)
        ├── KanbanBoard.jsx     # 4-колоночный канбан расписания (Предстоит, В процессе, Сделано, Отменено)
        ├── TaskModal.jsx       # Модалка быстрого добавления активности в расписание
        ├── NeedsPanel.jsx      # Интерактивные ползунки 6 потребностей с мгновенным сохранением и пресетами
        ├── FriendsPanel.jsx    # Социальный круг (Настя, Макс: дружба, драма, дедлайн, триггеры)
        ├── FullScreenMap.jsx   # Полноэкранная интерактивная карта СПб с плавающим HUD, Follow Cam и пешеходной навигацией
        ├── ApartmentInteriorModal.jsx # Уютная комната Леры на Петроградке (окно на крыши, рабочий стол, кровать, гардероб, HUD)
        ├── LeraCharacterBlock.jsx # Единый канонический профиль Леры (архетипы, проекции CHAT/CHANNEL/INIT, Focus-mode ⤢, сленг-теги, Live Dry-Run)
        ├── AiSettingsTab.jsx   # Настройки ИИ (Канбан пайплайна генерации, AI-судья, провайдеры, автопостинг, мониторинг)
        ├── Toast.jsx           # Всплывающие системные уведомления в стиле Linear
        └── LoginModal.jsx      # Форма входа по ADMIN_WEB_KEY
```

---

## 3. Табы и компоненты дашборда

### Вкладка 1: «Пульт» (Overview — точная верстка по макету Figma `13:1910`)
Контейнер экрана шириной 1005px (max-w-[1042px] c паддингами) на холсте `#0c0c0c`:
1. **Header (Figma 13:1935 / 13:1936):**
   - Липкий верхний бар (`sticky top-0 z-50 pointer-events-none`) с плавным градиентным фоном и размытием: `bg-gradient-to-b from-[#0c0c0c] via-[#0c0c0c]/85 to-transparent backdrop-blur-md` с плавной маской затухания, контент снизу плавно проезжает под шапку без залипания кликов.
   - Центрированная pill-группа навигации `pointer-events-auto bg-[#1b1d22]/90 backdrop-blur-xl rounded-full p-2 border border-white/[0.08] shadow-[0_8px_32px_rgba(0,0,0,0.6)]`.
   - Активная вкладка: `bg-[#292e5e] border border-[#434771] text-white rounded-full px-4 py-2 text-[16px]`.
   - Неактивные: `text-white/58 hover:text-white rounded-full px-4 py-2 text-[16px]`.
2. **LeraStatusHero (Figma 15:2765):**
   - Карточка-хиро `1005x250px`, `rounded-[19px]`, фон 3D-карты Петроградки с кликабельным Zenly-бейджем.
   - Центр: круглый аватар `w-[99px] h-[100px]`, имя «Лера» (`text-[24px]`), хэндл `@geexy_bot` (`text-[16px] text-white/50`).
   - Нижняя строка со статусами и Figma SVG-иконками: Время (`icon_clock.svg`), Погода (`icon_weather.svg`), Баланс (`icon_wallet.svg`), Цикл (`icon_calendar.svg`), Локация (`icon_nav.svg`).
3. **NeedsPanel (Figma 13:2132):**
   - Заголовок «Состояние» (`text-[20px] font-medium text-white`).
   - 6 карточек индикаторов `159x92px`, `rounded-[23px]`, `bg-[#000212]/37`, `border-[#8693ff]/25`.
   - Градиентные пилюли `h-[43px] rounded-[20px]` (Голод, Скука, Туалет, Усталость, Свежесть, Влечение) с прямым редактированием и быстрыми сценариями.
4. **KanbanBoard (Figma 13:2135):**
   - Заголовок «Расписание на день» + кнопка «+ Добавить задачу».
   - 4 колонки `245x351px`, `rounded-[23px]`: «Предстоит» (`#0b0d0f`), «В процессе» (`#000212`/37% + `#282f58`), «Сделано» (`#0c1113`), «Отменено» (`#100f13`).
   - Карточки задач `rounded-[20px] p-3 text-[15px]` с real-time очередью Radiant.
5. **InventoryWidget (Figma 14:2186):**
   - 2 колонки `492x287px`, `rounded-[18px]`, `bg-[#151515]`, `border-white/10`.
   - Слева «Надето», справа «Экипировка» с табами («Все», «Еда», «Одежда», «Девайсы», «Разное»).
   - Универсальный компонент строки `FigmaListItemRow` (Frame 221): `h-[61px]`, `rounded-[16px]`, иконка 9 точек `icon_dots9.svg`, кнопки «Снять», «Съесть», «Надеть», «Использовать» с реальными серверными мутациями.
6. **Дополнительно (Friends & Decisions Log):**
   - Аккордеон под инвентарем для детального инспектирования NPC-связей и журнала GOAP-решений.

- **ItemBuilderModal:** модалка создания предметов:
  - Независимая настройка категории, слота экипировки, многоразовости и количества.
  - Пресеты в 1 клик (Satisfyer 18+, Фильтр-кофе, Сырный Рамен, Питерский тренч).
  - Сгруппированный селект (`<optgroup>`) всех потребностей Радианта, настроения и погоды.
  - Единый знаковый инпут эффекта (`-50` снизить, `+20` поднять) без лишних тумблеров.
  - Кастомные SVG-шевроны с отступами `pl-3 pr-8 py-2` и лаконичная кнопка «Сохранить».

### Вкладка 2: «Настройка ИИ» (AiSettingsTab — точная верстка по макету Figma `13:1719`)
- **Точка входа:** `#ai` в URL или кнопка «Настройка ИИ» в верхнем сегментированном переключателе.
- **Ширина контейнера:** `max-w-[1042px] pl-[20px] pr-[17px] pt-[34px] pb-[60px] gap-[20px]` на холсте `#0c0c0c`.
- **Левая колонка — «Промпты» / «Провайдеры» (`w: 393px`, gap `24px`):**
  - **Дропдаун-переключатель в шапке:** клик по заголовку открывает всплывающее меню переключения режима левой колонки между «Промпты» и «Провайдеры».
  - Контекстная кнопка действия: «Новый промпт» (в режиме промптов) или «Новый провайдер» (в режиме провайдеров, `bg-[#292e5e] text-white`).
  - **Режим «Промпты»:**
    - **Приоритет канона (`sortPrompts`):** канон и системные промпты всегда идут первыми во всех списках («Канон и биография» #1, «Характер» #2, «Голос и речь» #3, далее Стиль А и системные модули, а пользовательские — в конце).
    - Карточки двух стилей (`w: 377px min-h-[158px] rounded-[23px]`):
      - *Стиль A (Канон):* `bg-[#000212]/37 border border-[#8693ff]/25`, внутренний блок `108px bg-[#1d1f2f]/39 text-white/72`.
      - *Стиль B (Модульные):* `bg-gradient-to-b from-[#171717] to-[#232425]/0 border border-white/10`, внутренний блок `108px bg-[#272727] text-white/45`.
      - Кнопка редактирования (карандаш) открывает модалку редактирования текста и стиля.
  - **Режим «Провайдеры»:**
    - Карточка основного активного провайдера (Стиль A `#000212/37` с неоновым акцентом `#8693ff]/30`): имя, модель, endpoint, таймаут, бейдж «Активен».
    - Список цепочки фоллбэков (Стиль B `bg-gradient-to-b from-[#171717] to-[#232425]/0 border border-white/10`):
      - Кнопки ↑ и ↓ для изменения порядка и приоритета фоллбэков (`PATCH /api/admin/providers/:id/priority`).
      - Кнопка «Сделать основным» для мгновенного переключения главного AI (`POST /api/admin/providers/:id/activate`).
      - Кнопки редактирования и удаления провайдера (`DELETE /api/admin/providers/:id`).
    - Модалка `ProviderModal` для создания и настройки нового провайдера (`POST /api/admin/providers`).
- **Правая колонка — «Правила» (`w: 592px`, gap `24px`):**
  - Селектор поверхностей: «Личка» (CHAT), «Тг-канал» (CHANNEL), «Инициатива» (INITIATIVE). Активная вкладка: `rounded-[12px] bg-[#232425] border border-[#353636]`.
  - Шапка правил: заголовок «Правила» + кнопка «Добавить правило» (`w: 178px, h: 38px, rounded-full bg-[#292e5e]`).
  - **Условия режимов (CASUAL / EROTIC / ALL):** в модалке правила задается целевой режим диалога («Все режимы», «Casual», «Erotic 18+»). На карточке выводится соответствующий бейдж.
  - **Провайдер и цепочка фоллбэков для правила:**
    - В модалке правила: селектор индивидуального основного провайдера (`provider_id` / «По умолчанию глобальный») и мульти-селектор цепочки фоллбэков (`fallback_provider_ids`) с нумерацией шагов.
    - На карточке правила отображается бейдж назначенного провайдера и количество резервных фоллбэков.
  - Карточки правил (`w: 586px min-h-[199px] rounded-[23px] bg-gradient-to-b from-[#171717] to-[#232425]/0 border border-white/10`):
    - Шапка: название правила + бейдж режима + круглая кнопка-чекмарк включения/выключения правила.
    - Строка информации о провайдере и фоллбэках правила.
    - Левый подблок (`w: 288px bg-[#272727] rounded-[20px]`): заголовок «Инструкции» + шеврон `>` (клик открывает модалку прикрепления модулей промптов), список прикрепленных карточек с 9 точками и карандашом.
    - Правый подблок (`w: 272px`): карточки параметров «Токены [N] >» и «Темп: [T] >» с всплывающими слайдерами для быстрого изменения на лету.
- **Бэкенд:** прямое чтение и сохранение через `/api/admin/lera-profile` (профиль, правила, токены, температура).

### Вкладка 3: «Карта СПб» — Интерактивная Zenly-карта (ZenlyMap)
- **Стиль Zenly:** пухлые круглые аватары персонажей с пульсирующим радар-кольцом, динамический % заряда батареи с цветовой кодировкой, статус-пилюли с активностью и скоростью, метка времени на месте («Здесь 35м»).
- **Двухдвижковая архитектура (Dual Engine):**
  - *Основной:* Яндекс Карты JS API 3.0 (`ymaps3`) с вектором, адаптивной темой и 3D зданиями с наклоном 42° (`tilt: (42 * Math.PI) / 180`).
  - *Graceful Fallback:* MapLibre GL с Carto Voyager (день) / Carto Dark Matter (ночь) и 3D extrusions на случай сетевых ограничений или отсутствия referer.
  - *Ручной переключатель:* кликабельный бейдж в верхнем HUD («Яндекс 3D» ⇄ «MapLibre 3D») и переключатель световых тем (☀️ День / 🌅 Закат / 🌙 Ночь / ⚡ Авто по `calculateSpbSun()`).
- **Честный транзит (Transit Engine):**
  - Клик по точке назначения не телепортирует Леру мгновенно, а открывает карточку маршрута с расчетом дистанции (км), времени пешком и цены такси.
  - При выборе пути («🚶‍♀️ Идти» или «🚕 Заказать такси») отправляется `taskType: 'TRAVEL'` в Radiant симуляцию.
  - На карте строится неоновая пунктирная линия пути (`route-path-source`, GeoJSON LineString).
  - Прогресс транзита (0% → 100%) честно тикает в симуляции (+15 мин), статус переходит в `IN_TRANSIT`, и персонаж перемещается только по прибытии.
- **Стабильная фиксация 14 меток СПб:**
  - База из 14 проверенных локаций СПб (Дом на Петроградке, СПбГИК, Севкабель, Новая Голландия, кофейня Слой, Сенная, Рубинштейна и др.).
  - Строгая фиксация маркеров (`anchor: 'bottom'`) без конфликтующих `transform: translate`, метки жестко закреплены на фасадах при любом наклоне и зуме.
  - Модальный каталог `ZenlyPlacesCatalogModal` с поиском, категориями и дистанциями от Леры.
- **Очистка от оверинжиниринга и контекстные действия:**
  - Полностью удален визуальный шум: фейковые фонтанчики эмодзи-реакций и неработающая кнопка «Написать в TG».
  - В локациях добавлены честные контекстные действия (купить кофе/круассан, смена в шоуруме, учеба в СПбГИК, сон/душ дома), мутирующие потребности Леры.


---

## 4. Правила разработки UI (`better-ui` & `better-layout`)

1. **Концентрический радиус скругления:** `outerRadius = innerRadius + padding` (доки `rounded-3xl` при `p-3` и child `rounded-xl`; колонки `rounded-2xl` при `p-2.5` и child `rounded-md`).
2. **Тактильный отклик (Scale on Press):** `active:scale-[0.96]` со специфичным `transition-[...,transform] duration-150` на всех кнопках.
3. **Запрет `transition: all`:** объявлять только целевые анимируемые свойства (`transition-colors`, `transition-[background-color,border-color,color,transform]`).
4. **Иконки:** везде `stroke-[1.5]` для визуального баланса со шрифтом Inter Variable.
5. **Группировка пространством:** минимум внутренних разделительных линий; разделение за счет отступов (gap).
6. **Контрастность:** микро-текст строго `text-white/50` или выше (WCAG AA).
7. **Хоткеи:**
   - `T` — шаг +15 мин
   - `Space` — пауза / старт
   - `R` — обновить данные
   - `M` — быстро переключить вкладку (Пульт ↔ Карта)

---

## 5. 3D Симулятор Петроградки (MapLibre GL + Three.js)

1. **Архитектура WebGL и глубина:**
   - MapLibre GL с `renderingMode: '3d'` шарит WebGL depth buffer с Three.js (`renderer.autoClear = false`).
   - Координатная сетка привязана к центру Петроградки `[30.3080, 59.9590]` через матрицу Меркатора (1 unit = 1 meter).
2. **Фасады и крыши (Milestone M1):**
   - Процедурный атлас классицистических фасадов Петербурга (`pixelRatio: 32`): рустованный гранитный цоколь, классические окна с наличниками и подоконниками, арочные парадные порталы.
   - Слой фальцевых графитовых крыш `3d-buildings-roof` (+0.12m над зданиями) полностью исключает наложение окон на крыши.
   - Белая разметка пешеходных переходов (зебры) и прерывистые осевые линии на проспектах.
3. **GTA 3D персонажи и NavMesh (Milestone M2):**
   - 12-составная biped-иерархия с синусоидальной биомеханической кинематикой шага.
   - Вращающийся изумрудный Sims Plumbob над Лерой.
   - 4-уровневый NavMesh с физической проверкой коллизий по 1080 зданиям Петроградки — 0.000% коллизий с домами.
   - Физические 3D магазины и кафе с ночными PointLight источниками («Слой», «ВкусВилл», «Шоурум»).
   - Поддержка открытых 3D моделей GLB (`michelle.glb`, `lera.glb`, `citizen.glb`) с процедурным fallback.
4. **Городская среда и трафик (Milestone M3):**
   - Деревья строго внутри парковых полигонов (Матвеевский сад, сквер Низами) и в тротуарных лунках Большого проспекта с контролем минимальной дистанции Пуассона — 0 деревьев на крышах и во дворах.
   - Чугунные фонари освещения и дорожная разметка строго по поребрикам и полосам движения проспектов — 0 фонарей и переходов на газонах парков.
   - Автомобильный трафик (такси, седаны, лазурный автобус СПб) по точным дорожным сплайнам Большого и Каменноостровского с физикой Intelligent Driver Model (IDM) и объемными световыми лучами фар.
   - Астрономический калькулятор солнца СПб (`solarCalculator.js`): автоматический расчет угла высоты, азимута и длины теней в реальном времени.
   - Сбалансированное озеленение Петроградской стороны (`cityEnvironmentData.js`): Матвеевский сад (35 деревьев), сквер Низами (40 деревьев), Александровский парк (95 деревьев) и аллеи Большого и Каменноостровского проспектов (`PETROGRADKA_TREES`).

5. **Пешеходная навигация и Follow Cam (`pedestrianData.js`, `FullScreenMap.jsx`):**
    - Единая 100% связная топология тротуаров через Каменноостровский проспект и узел дома Леры без коллизий со зданиями.
    - Поиск кратчайшего пути по тротуарам `findNavMeshPath` и транзит `createLeraTransit` / `interpolateTransitPosition`.
    - Плавный цикл ходьбы (biped gait), синхронизированный бабл цели над головой («Иду в «Слой» ☕», «Бегу в СПбГИК 🎓»).
    - Режим следящей камеры від 3-го лица (Follow Cam) с автоотключением при ручном перетаскивании карты.

6. **Полноценная 3D Комната Леры (`admin-linear/src/lib/threeRoom/`, `ApartmentInteriorModal.jsx`):**
    - **Архитектура старого фонда (`RoomArchitectureBuilder.js`):** $6.0 \times 5.0 \times 3.6\,\text{м}$ (высокие исторические потолки), процедурный дубовый паркет «ёлочкой», тёплые штукатурные стены, высокие плинтусы ($0.22\,\text{м}$), лепные карнизы, арочное полуциркульное окно с глубоким деревянным подоконником ($0.45\,\text{м}$) и остеклением.
    - **3D-Аватар Леры (`LeraCharacter.js`):** рост $1.71\,\text{м}$, графитовое оверсайз худи, синий деним, белые ретро-кеды, каштановые волосы; процедурные кинематические циклы шага (`walkPhase`), дыхания (`idlePhase`), позы прыжка и парящий изумрудный Sims Plumbob. Поддерживает загрузку `/models/lera.glb` через GLTFLoader с процедурным fallback.
    - **Мебель и декор (`FurniturePropsBuilder.js`):**
      - Рабочий стол ясеня, кресло, MacBook со светящимся экраном (эмиссионный свет с интерфейсом кода/студии), чашка кофе, блокнот с ручкой.
      - Кровать с изголовьем, льняным бельем, серым шерстяным пледом, подушками и тумбочкой с ночником.
      - Винтажный латунный торшер с абажуром и тёплым светом ($2700\,\text{K}$).
      - Индустриальный рейл с плечиками и одеждой (бежевое пальто, графитовое худи, жёлтый дождевик).
      - Книжный стеллаж с книгами и керамической вазой.
      - Монстера в терракотовом горшке на подоконнике.
      - Мягкий шерстяной ковёр с геометрическим узором.
      - Запечённые контактные тени под мебелью для гарантированных $60\text{--}120\,\text{FPS}$.
    - **Окружение Петербурга (`SkylineEnvironment.js`):** динамический небосвод СПб (рассвет, день, закат, ночь), силуэты фальцевых крыш, печных труб, шпиля Петропавловки и светящихся слуховых окон мансард.
    - **Физика, коллизии и прыжки (`CollisionSystem.js`):**
      - Непрерывный 2D AABB тест с раздельным скольжением по осям (Axis-Separated Sliding Plane).
      - Учет высот препятствий (`getGroundElevation`): прыжок на кровать ($0.55\,\text{м}$), стол ($0.75\,\text{м}$) и кресло с сохранением ходьбы по ним и реалистичным падением при сходе.
    - **Режимы камеры и управление (`CameraController.js`):**
      - *1-е лицо:* уровень глаз $1.62\,\text{м}$, $\text{FOV} = 75^\circ$, $\text{near} = 0.08\,\text{м}$, повышенная скорость перемещения ($3.2\,\text{м/с}$ шаг, $5.8\,\text{м/с}$ спринт на Shift), физика прыжков на Space ($v_y = 5.2\,\text{м/с}$, гравитация $18\,\text{м/с}^2$), захват мыши PointerLock ($\lambda_{rot} = 28$).
      - *The Sims:* изометрия под $45^\circ$, срез ближней стены/потолка, Point-and-Click навигация (клик по полу пускает импульсный маркер, персонаж поворачивается и идет к цели), колесико мыши/пинч для зума (приближение/отдаление $0.5\text{--}1.9$), правая кнопка мыши для орбитального поворота вокруг комнаты.
    - **Инженерный UX (Emil Kowalski Craft):** двухступенчатый Esc, экранный тулбар зума (+, -, сброс), кнопки прыжка и поворота ракурса, переключатели светильников (торшер, ночник, экран), и 100% освобождение ресурсов GPU при `dispose()`.
